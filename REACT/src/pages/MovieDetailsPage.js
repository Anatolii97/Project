import { MovieDetailsContainer } from "../components/MovieDetails/MovieDetails"

export const MovieDetails = ({match, history}) => {
    return <MovieDetailsContainer match={match} history={history} />
}